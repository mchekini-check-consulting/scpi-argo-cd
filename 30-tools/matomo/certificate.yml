apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: matomo-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: matomo-tls
  dnsNames:
    - matomo.check-consulting.net
  usages:
    - digital signature
    - key encipherment
