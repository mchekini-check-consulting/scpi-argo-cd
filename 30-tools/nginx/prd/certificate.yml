apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: nginx-tls
  dnsNames:
    - nginx.check-consulting.net
  usages:
    - digital signature
    - key encipherment
