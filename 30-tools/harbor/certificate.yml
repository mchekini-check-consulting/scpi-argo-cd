apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: harbor-tls
  dnsNames:
    - harbor.check-consulting.net
  usages:
    - digital signature
    - key encipherment
