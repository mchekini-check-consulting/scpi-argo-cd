apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pg-db-tls-int
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: pg-db-tls-int
  dnsNames:
    - int.pg-db.check-consulting.net
  usages:
    - digital signature
    - key encipherment
