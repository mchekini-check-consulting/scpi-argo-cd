apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pg-db-tls-qua
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: pg-db-tls-qua
  dnsNames:
    - qua.pg-db.check-consulting.net
  usages:
    - digital signature
    - key encipherment
